[project]
name = "n6k-app"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "n6k>=0.6.1",
]

# This is an application, not a library — uv manages the dependency env
# (so `uv run n6k dev` works) without trying to build/install the project itself.
[tool.uv]
package = false
